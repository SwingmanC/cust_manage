<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.custmanage.server.mapper.LoginLogMapper">

    <insert id="insert">
        INSERT INTO t_yqyc_sys_login_log (user_id, login_account, login_time, login_ip, login_result, fail_reason)
        VALUES (#{userId}, #{loginAccount}, NOW(), #{loginIp}, #{loginResult}, #{failReason})
    </insert>

</mapper>
